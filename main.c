#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "tree.h"

int main(){
  Tree * tree;
  Data data=10;
  InitialiseTree(tree);
 // AddDataIntoTree(tree,data);
 printf("ok\n");
  return 0;
}